# Copy to .dev.vars for `wrangler pages dev`. Never committed (.gitignore). Production values are set
# in the Cloudflare dashboard under the Pages project > Settings > Variables and Secrets.
SESSION_SECRET=replace-with-a-long-random-string
WORKER_URL=https://crimson-hat-6ad9.northbridgeai1.workers.dev
SYNC_SECRET=the-same-value-the-worker-has
